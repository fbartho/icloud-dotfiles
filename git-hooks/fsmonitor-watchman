#!/bin/sh
# Delegates to the repository-local fsmonitor-watchman hook under a global core.hooksPath.
# This name carries no global check; only commit-msg does.

HOOK_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
HOOK_NAME=fsmonitor-watchman
. "$HOOK_DIR/_delegate.sh" "$@"
