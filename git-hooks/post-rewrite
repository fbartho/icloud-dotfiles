#!/bin/sh
# Delegates to the repository-local post-rewrite hook under a global core.hooksPath.
# This name carries no global check; only commit-msg does.

HOOK_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
HOOK_NAME=post-rewrite
. "$HOOK_DIR/_delegate.sh" "$@"
