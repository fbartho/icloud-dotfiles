#!/bin/sh
# Delegates to the repository-local post-checkout hook under a global core.hooksPath.
# This name carries no global check; only commit-msg does.

HOOK_DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
HOOK_NAME=post-checkout
. "$HOOK_DIR/_delegate.sh" "$@"
